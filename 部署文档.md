# 部署文档（Linux 服务器 Docker，不走镜像仓库）

> 个人项目，不使用公司阿里云镜像仓库。流程：本机构建镜像 → `docker save` / `scp` / `docker load` 直传服务器 → `docker compose up`。
>
> 运行时依赖只有 **MySQL + Redis**（已有实例，直接连接）。Nacos 配置在代码中无实际使用，可忽略。
> `.dockerignore` 已排除 `.env`，密钥不会打进镜像；所有配置通过服务器上的 `env_file: .env` 注入。

## 一、本机构建镜像

```powershell
cd D:\zym\项目\git\shcoolInfo
docker build -t shcoolinfo:v1.0.0 .
```

> ⚠️ `COPY . /app` 会把**工作区当前代码（含未提交改动）**打进镜像，构建前确认要上线的版本。
> 本地 `.env` 不会被拷入镜像（`.dockerignore` 已排除），构建不依赖本地配置。

## 二、导出镜像并传到服务器

```powershell
# 导出（gzip 压缩，体积通常几百 MB 以内）
docker save shcoolinfo:v1.0.0 | gzip > shcoolinfo-v1.0.0.tar.gz

# 传到服务器（替换为实际服务器地址/用户）
scp shcoolinfo-v1.0.0.tar.gz user@<server-ip>:/opt/shcoolinfo/
```

服务器上加载：

```bash
gunzip -c /opt/shcoolinfo/shcoolinfo-v1.0.0.tar.gz | docker load
docker images | grep shcoolinfo   # 确认已加载
```

## 三、服务器上准备部署目录

目录 `/opt/shcoolinfo/`（与 scp 目标一致），放两个文件：

### 1. `docker-compose.yml`

用仓库里这份（镜像名已改为本地名 `${PROJECT_NAME:-shcoolinfo}:${IMAGE_TAG:-latest}`）。
`network_mode: host` 在 Linux 下正常工作，容器通过宿主机网络直连 MySQL/Redis。

### 2. `.env`

```ini
DB_HOST=<已有MySQL地址>
DB_PORT=3306
DB_USER=xxx
DB_PASSWORD=<密码>          # prod 模式强制要求，缺失拒绝启动
DB_NAME=xxx

REDIS_HOST=<已有Redis地址>
REDIS_PORT=6379
REDIS_PASSWORD=<密码>       # prod 模式强制要求
REDIS_DB=0

PROJECT_NAME=shcoolinfo
APP_PORT=8000               # compose 里 gunicorn 实际监听的端口
IMAGE_TAG=v1.0.0            # 必须与镜像 tag 一致，否则找不到镜像

RUN_MODE=prod               # 不写默认也是 prod（app/core/config.py）
LOG_LEVEL=INFO
```

## 四、启动

```bash
cd /opt/shcoolinfo
docker compose up -d
docker compose logs -f --tail=100
# 确认日志出现 "Application starting up..." 与 "✅ 生产环境安全配置验证通过"
```

## 五、验证

在服务器（或任何能访问该端口的主机）上：

1. `curl http://127.0.0.1:8000/` → 返回 `{"message": "Welcome to Python Web!"}`
2. 浏览器访问 `http://<server-ip>:8000/ui/schools` → 学校名录页正常渲染、搜索可用
3. `curl http://127.0.0.1:8000/docs` → FastAPI 文档页可打开
4. `ls /opt/shcoolinfo/logs/` → `app_*.log` 正常落盘（compose 已挂载 `./logs:/app/logs`）
5. 接口抽查：任一 API 返回 `{"errCode": ..., "errMsg": ..., "data": ...}` 统一结构

## 六、后续版本更新

```text
本机: docker build -t shcoolinfo:v1.0.1 .
      docker save shcoolinfo:v1.0.1 | gzip > shcoolinfo-v1.0.1.tar.gz
      scp shcoolinfo-v1.0.1.tar.gz user@<server-ip>:/opt/shcoolinfo/

服务器: gunzip -c /opt/shcoolinfo/shcoolinfo-v1.0.1.tar.gz | docker load
        改 .env 中 IMAGE_TAG=v1.0.1
        docker compose up -d        # 自动重建容器
```

传完的 `.tar.gz` 可在服务器上删除；旧镜像 tag 保留用于回滚。

## 七、回滚

把 `.env` 的 `IMAGE_TAG` 改回旧 tag，然后 `docker compose up -d`。

## 常见问题

| 现象 | 原因 / 处理 |
| --- | --- |
| 启动报 `生产环境Redis密码未配置，禁止启动` | `.env` 缺 `REDIS_PASSWORD`（prod 模式强制校验） |
| 启动报 `生产环境数据库密码未配置，禁止启动` | `.env` 缺 `DB_PASSWORD` |
| `docker compose up` 报找不到镜像 | `.env` 的 `IMAGE_TAG` 与 `docker load` 进来的 tag 不一致 |
| 容器起了但连不上 MySQL/Redis | `network_mode: host` 下用宿主机网络，确认地址可达、防火墙放行 |
| 端口被占用 | 改 `.env` 的 `APP_PORT` 后 `docker compose up -d` 重建 |

## 不需要做的事

- ❌ 不 push/pull 任何 registry
- ❌ 不配置 Nacos（代码未使用）
- ❌ 服务器上不装 Poetry/Python（Dockerfile 内已装好依赖）
- ❌ 容器内不手动 commit/rollback DB（异步引擎依赖退出时自动处理）
