# 香港插班学校名录

为需要插班的家长提供全港学校信息查询的产品。数据覆盖香港中学（约 480 所）与小学（约 550 所）。

## Language

**学校 (School)**:
一所香港学校的基本信息记录，含名称、官网、类型、地区、学段、组别、校网、教学语言、性别、宗教、地址、电话、电邮。

**学段 (Stage)**:
`schools.stage` 字段，`secondary`=中学 / `primary`=小学。列表页按学段切换，筛选维度随之联动（组别仅中学，校网仅小学）。

**校网 (School Net)**:
`schools.school_net` 字段，小一派位校网编号（11–98），仅小学有值。值为 `'0'` 表示直资/私立小学，不参与派位校网，展示为"—"；中学该字段恒为 NULL。
_Avoid_: 学区、片区

**插班 (Transfer Admission)**:
学生在非起始年级转入另一所学校的入学申请，是本产品服务的核心场景。
_Avoid_: 转学、插班生申请

**插班链接 (Admission Link)**:
某个学校的插班申请入口 URL。一所学校可有多条（如按年级分入口），也可一条都没有；链接以 `admission_links` 表为唯一权威来源。
_Avoid_: schools.admission_link 字段（已废弃，不再使用）

**官网 (Official Website)**:
学校的官方主页 URL，与插班链接是两种独立的外链。

**组别 (Banding)**:
香港中学的派位组别（Band 1/2/3，如 1A），家长择校时常用的比较维度。仅中学有值，小学恒为空。

**年级 (Grade)**:
插班链接标注的适用年级（`admission_links.grades`，逗号分隔）。取值为 `P1`–`P6`（小一至小六）/ `S1`–`S6`（中一至中六）；`all` 表示链接未标注年级。少量链接跨学段（如一条龙小学挂 `S1` 入口），属正常数据。前端展示转译为「小一、中一」等中文。
